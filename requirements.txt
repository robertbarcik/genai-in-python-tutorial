jupyter
notebook
nbformat
openai==3.13.0
pydantic==2.12.3
pandas==2.2.2
numpy==2.0.2
matplotlib==3.10.0
pillow==11.3.0

# Module 7 (Hugging Face) needs a GPU and installs its own extra packages
# (transformers, torch, accelerate, etc.) inline when run in Google Colab.
# They are not included here to keep local installs light.
